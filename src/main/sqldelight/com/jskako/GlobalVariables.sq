CREATE TABLE GlobalVariables (
    identifier TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);

insert:
INSERT OR REPLACE INTO GlobalVariables(identifier, value)
VALUES(?,?);

updateValue:
UPDATE GlobalVariables
SET value = :value
WHERE identifier IS :identifier;

delete:
DELETE FROM GlobalVariables
WHERE identifier IS :identifier;